name: Lua Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: lunexor
    steps:
      - uses: actions/checkout@v3
      - uses: leafo/gh-actions-lua@v9
      - uses: leafo/gh-actions-luacheck@v4
