name: Test Release Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  # Erlaubt den Aufruf durch andere Workflows (z.B. Release)
  workflow_call:

jobs:
  test_release_workflow:
    runs-on: 'lunexor'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Nutze Python 3.11 mit Pip Caching für schnellere Builds
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: python -m pip install --upgrade pip pyyaml

      # Führe Workflow-Validierung aus
      - name: Run release.yml unit tests
        id: run_tests
        env:
          WORKFLOW_PATH: .github/workflows/release.yml
        run: |
          python scripts/validate_release_workflow.py

      # --- Notifications ---

      # Discord Notification: Erstelle Payload als Datei (verhindert Escaping-Probleme)
      - name: Prepare Discord Payload
        if: always()
        id: discord_payload
        run: |
          STATUS="${{ job.status }}"
          
          # Setze Farbe und Titel basierend auf Status
          if [ "$STATUS" == "success" ]; then
            COLOR=3066993  # Grün
            TITLE="✅ Workflow Validierung erfolgreich"
            DESC="Das Release-Workflow-Skript wurde erfolgreich validiert."
          else
            COLOR=15158332  # Rot
            TITLE="❌ Workflow Validierung fehlgeschlagen"
            DESC="**⚠️ Fehler:** Die Validierung des Release-Workflows ist gescheitert."
          fi
          
          # Erstelle JSON Payload in temporärer Datei
          cat <<EOF > payload.json
          {
            "username": "MM Megaphone CI/CD",
            "avatar_url": "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg",
            "embeds": [{
              "title": "$TITLE",
              "description": "$DESC",
              "color": $COLOR,
              "fields": [
                { "name": "🧪 Test", "value": "\\`validate_release_workflow.py\\`", "inline": true },
                { "name": "🌿 Branch", "value": "\\`${{ github.ref_name }}\\`", "inline": true },
                { "name": "🔗 Logs", "value": "[View Logs](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})", "inline": true }
              ],
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)"
            }]
          }
          EOF

      - name: Notify Discord
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          # Sende Payload aus Datei (verhindert Escaping-Probleme)
          curl -s -F "payload_json=$(cat payload.json)" "$DISCORD_WEBHOOK"

      - name: Notify Telegram
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_API_TOKEN }}
          format: markdown
          message: |
            ${{ job.status == 'success' && '✅ *Workflow Validierung erfolgreich*' || '❌ *Workflow Validierung fehlgeschlagen*' }}
            
            *Test:* `validate_release_workflow.py`
            *Branch:* `${{ github.ref_name }}`
            
            [🔍 Logs ansehen](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
